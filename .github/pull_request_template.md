## Summary

- What changed and why?

## Test plan

- [ ] `npm test`
- [ ] `npm run test:e2e` (if UI or flows touched)

## Checklist

- [ ] No secrets in diff (`.env`, keys, `cos_` keys in fixtures)
- [ ] EN + IT strings updated when user-facing copy changed
- [ ] `STATUS.md` / `CHANGELOG.md` if release-facing
